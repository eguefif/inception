version: '3.1'
services:
  nginx:
    build: ./requirements/nginx/
    ports:
      - "443:443"
      - "80:80"
    networks:
      - inception

  mariadb:
    build: ./requirements/mariadb
    env_file:
      ./.env
    ports:
      - "3306:3306"
    volumes:
      - mariadb:/var/lib/mysql
    networks:
      - inception



  wordpress:
    build: ./requirements/wordpress
    depends_on:
      - mariadb
      - nginx
    networks:
      - inception
    volumes:
      - wordpress:/var/www/

volumes:
  mariadb:
    name: mariadb
    driver: local
    driver_opts:
      type: none
      o: bind
      device: "/home/eguefif/data/mariadb"
  wordpress:
    name: wordpress
    driver: local
    driver_opts:
      type: none
      o: bind
      device: "/home/eguefif/data/wordpress"

networks:
  inception:
    name: inception
